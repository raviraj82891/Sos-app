<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.EmergencyMesh" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary color -->
        <item name="colorPrimary">@color/neon_blue</item>
        <item name="colorOnPrimary">@color/text_white</item>

        <!-- Background/Surface -->
        <item name="android:windowBackground">@drawable/bg_main_gradient</item>
        <item name="colorSurface">@color/bg_gradient_start</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/bg_gradient_start</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/bg_gradient_end</item>
    </style>
</resources>